{% extends "layout.njk" %}

{% block title %}{{ jobRole.name }} - Kainos Careers{% endblock %}

{% block content %}
<div class="container mx-auto px-4 py-8 max-w-6xl">
  <!-- Page Title -->
  <h1 class="text-6xl font-bold mb-8 text-center kainos-text-gradient">Job Details</h1>
  <p class="text-center text-lg text-base-content/70 mb-8">Join the Kainos team and make an impact</p>
  
  <!-- Success Message (shown when application is submitted) -->
  {% if applicationSubmitted %}
  <div class="alert alert-success shadow-lg mb-8">
    <div>
      <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
      <div>
        <h3 class="font-bold">Application Submitted Successfully!</h3>
        <div class="text-sm">Thank you for applying. We'll review your application and get back to you soon.</div>
      </div>
    </div>
  </div>
  {% endif %}
  
  <div class="card bg-base-100 shadow-xl">
    <div class="card-body">
      <!-- Job Role Name -->
      <h2 class="text-4xl font-bold mb-8 text-center text-kainos-blue">{{ jobRole.name }}</h2>
      
      <!-- Key Information Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-10">
        <div class="space-y-8">
          <div>
            <h3 class="text-xl font-semibold text-primary mb-3">Location</h3>
            <p class="text-xl">{{ jobRole.location }}</p>
          </div>
          
          <div>
            <h3 class="text-xl font-semibold text-primary mb-3">Capability</h3>
            <p class="text-xl">{{ jobRole.capability }}</p>
          </div>
          
          <div>
            <h3 class="text-xl font-semibold text-primary mb-3">Band</h3>
            <p class="text-xl">{{ jobRole.band }}</p>
          </div>
          
          <div>
            <h3 class="text-xl font-semibold text-primary mb-3">Number of Open Positions</h3>
            <p class="text-xl">{{ jobRole.openPositions or 'Not specified' }}</p>
          </div>
        </div>
        
        <div class="space-y-8">
          <div>
            <h3 class="text-xl font-semibold text-primary mb-3">Closing Date</h3>
            <p class="text-xl">{{ jobRole.closingDate.toLocaleDateString() if jobRole.closingDate else 'Not specified' }}</p>
          </div>
          
          <div>
            <h3 class="text-xl font-semibold text-primary mb-3">Status</h3>
            <div class="badge badge-xl {% if jobRole.status == 'Open' %}badge-success{% elif jobRole.status == 'Closing Soon' %}badge-warning{% else %}badge-error{% endif %}">
              {{ jobRole.status or 'Active' }}
            </div>
          </div>
          
          {% if jobRole.jobSpecUrl %}
          <div>
            <h3 class="text-xl font-semibold text-primary mb-3">Job Specification</h3>
            <a href="{{ jobRole.jobSpecUrl }}" target="_blank" rel="noopener noreferrer" class="btn btn-outline">
              View Job Spec (SharePoint)
            </a>
          </div>
          {% endif %}
        </div>
      </div>
      
      <!-- Description Section -->
      {% if jobRole.description %}
      <div class="mb-10">
        <h3 class="text-3xl font-semibold text-primary mb-6">Job Description</h3>
        <div class="bg-base-200 p-8 rounded-lg">
          <div class="prose max-w-none">
            <p class="text-xl leading-relaxed">{{ jobRole.description }}</p>
          </div>
        </div>
      </div>
      {% endif %}
      
      <!-- Responsibilities Section -->
      {% if jobRole.responsibilities %}
      <div class="mb-2">
        <h3 class="text-3xl font-semibold text-primary mb-6">Key Responsibilities</h3>
        <div class="bg-base-200 p-8 rounded-lg">
          <div class="prose max-w-none">
            {% if jobRole.responsibilities is string %}
              <p class="text-xl leading-relaxed">{{ jobRole.responsibilities }}</p>
            {% else %}
              <ul class="text-xl leading-relaxed space-y-3">
                {% for responsibility in jobRole.responsibilities %}
                  <li>{{ responsibility }}</li>
                {% endfor %}
              </ul>
            {% endif %}
          </div>
        </div>
      </div>
      {% endif %}
    </div>
  </div>
</div>

<!-- Action Buttons - Outside container margins -->
<div class="mt-6 flex justify-center gap-4 flex-wrap">
  {% if jobRole.status == 'Open' or jobRole.status == 'Closing Soon' or not jobRole.status %}
  <a href="/jobs/{{ jobRole.id }}/apply" class="bg-blue-800 hover:bg-blue-900 text-white font-semibold px-6 py-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 hover:scale-105 min-w-[180px] no-underline text-center">
    Apply for This Position
  </a>
  {% else %}
  <div class="bg-gray-400 text-white font-semibold px-6 py-3 rounded-lg shadow-md min-w-[180px] cursor-not-allowed text-center">
    Applications Closed
  </div>
  {% endif %}
  
  <!-- Edit Button -->
  <a href="/jobs/{{ jobRole.id }}/edit" class="bg-blue-700 hover:bg-blue-800 text-white font-semibold px-6 py-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 hover:scale-105 min-w-[160px] no-underline text-center inline-block">
    Edit Job Role
  </a>
  
  <!-- Delete Button -->
  <button onclick="confirmDelete('{{ jobRole.id }}', '{{ jobRole.name }}')" class="bg-red-600 hover:bg-red-700 text-white font-semibold px-6 py-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 hover:scale-105 min-w-[160px]">
    Delete Job Role
  </button>
</div>

<!-- Back to Job List Button -->
<div class="mt-8 flex justify-center">
  <a href="/jobs" class="bg-blue-800 hover:bg-blue-900 text-white font-semibold px-6 py-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-200 hover:scale-105 min-w-[160px] no-underline text-center">← Back to Job List</a>
</div>

<!-- Hidden form for DELETE request -->
<form id="deleteForm" method="POST" style="display: none;">
</form>

<script>
function confirmDelete(jobId, jobName) {
  if (confirm(`Are you sure you want to delete the job role "${jobName}"? This action cannot be undone.`)) {
    const form = document.getElementById('deleteForm');
    form.action = `/jobs/${jobId}/delete`;
    form.submit();
  }
}
</script>
{% endblock %}